{% block body %}
<h1>Contact</h1>
<div class="form-row">
    <div class="form-group col-lg-6">
        <div class="formulaire text-center">{{form_row(form.firstname)}} {{form_row(form.lastname)}} {{form_row(form.email)}} {{form_row(form.phone_number)}}</div>

        <div class="message text-center mb-3"> {{form_row(form.message, { 'attr': {'style': 'height: 25rem'} })}}</div>
    </div>
</div>
{{form_row(form.checkbox)}}
<button class="btn mt-3" style="background-color:white">{{ button_label|default('Envoyer') }}</button> {% endblock %}